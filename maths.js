console.log("Mathematics Guru");
console.log(Math);
// Getting the value of E
console.log(Math.E);
console.log(Math.PI);

// Assignment number 1 find the min numbers in the numbers array
const numbers = [1, , 5, 2, 6, -9];

// Math.min
console.log(Math.min(1, 5, 2, 6, -9));

// Math.max
console.log(Math.max(1, 5, 2, 6, -9));

// Getting the power of a number
console.log(Math.pow(3, 3));

// Getting the sqrt of a number
console.log(Math.sqrt(144));

// Getting the absolute value of a number
console.log(Math.abs(-4));
console.log(Math.abs(10));

// Generate a random number
console.log(Math.random());

// Math.round
console.log(Math.round(4.2));

// Math.ceil
console.log(Math.ceil(4.3));

// MAth.floor
console.log(Math.floor(4.7));

// Assignment Number 2
// Write a code, that generates a random number between 1 and 40, and perfomrs the following operations on the number
// 1. finds the square root
// 2. Gets the absolute value
// 3. Rounds it to the nearest integer
// 4. ROund it UP to the nearest integer
// 5. ROund it down to the nearest integer
